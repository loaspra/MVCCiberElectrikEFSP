@model IEnumerable<pe.com.cibereletrik.marca>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nommar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estmar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nommar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estmar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codmar }) |
            @Html.ActionLink("Details", "Details", new { id=item.codmar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codmar }) |
            @Html.ActionLink("Enable", "Enable", new { id=item.codmar })
        </td>
    </tr>
}

</table>
